import { Box, Container, Input, Text } from '@chakra-ui/react';
import React from 'react';

const SalesId = () => {
  return (
    <Container>
      <Box my={6}>
        <Text>売上</Text>
        <Input />
      </Box>
    </Container>
  );
};

export default SalesId;
